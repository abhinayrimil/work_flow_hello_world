name: print-name

on:
  push:
    branches:
      - main

jobs:
  print-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello Message
        run: echo "Hello World - Workflow Executed Successfully"
